import 'package:flutter/material.dart';

class ProfileScreren extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ProfileScreen"),
      ),
    );
  }
}
